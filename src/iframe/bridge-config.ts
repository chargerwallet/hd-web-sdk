const config: {
  iframeName: string;
  hostName: string;
  channel: string;
  scope: '$hardware_sdk';
} = {
  iframeName: 'chargerwallet-iframe',
  hostName: 'chargerwallet-host',
  channel: 'chargerwallet-js-sdk',
  scope: '$hardware_sdk',
};

export default config;
